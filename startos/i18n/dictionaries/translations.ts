import { LangDict } from './default'

/**
 * Translations are keyed by the numeric ids in ./default.ts.
 * English-only for now; contributions welcome.
 */
export default {} satisfies Record<string, Record<number, string>>
